동작 버그 5건 RCA 후 수정 PR 진행해줘.

## 입력
- docs/plans/2026-04-29-roadmap-review-findings.md

## 공통 진행 규칙
1. 시작 시 findings.md 를 먼저 읽고 본 라운드 범위 + 이미 [해결됨] 표시된 항목 + 다른 라운드 메모 확인
2. 작업 중 신규 결함 발견 시 findings.md 의 "## 추가 발견 사항" 섹션에 추가
3. 작업 중 다른 라운드 항목에 영향 주는 사실 발견 시 해당 항목에 "> 메모(YYYY-MM-DD, R4로부터 발견): 내용" 추가
4. PR 머지 직전: 본 PR 이 닫는 항목에 [해결됨][PR #N] YYYY-MM-DD 표기 + findings.md 변경 같은 PR 에 포함
5. 머지는 사용자 명시 승인 후에만 진행

## 본 라운드 (R4) 범위
- #4 Ⅰ-2 '+ 차수 추가' 버튼 무동작
- #6 Ⅱ-2 비고 작성 불가
- #13 결과 페이지 탭 전환 딜레이
- ~~#20 HWPX 3.과업·워크플로우 분석 표 전체 공란~~ — **R3 PR2 (PR #43) 에서 해결됨.**
  근본 원인: hwpx-payload-roadmap.ts 가 V1 키 (job·as_is·problems) 만 가정해 V2 마이그 후 production DB 키 (roadmap_job·task_description·roadmap_problems·...) 미인식. V2 우선 + V1 fallback 으로 통일 처리. **R4 범위에서 제외.**
- ~~#21 HWPX 4.훈련대상 과업·워크플로우 선정 누락~~ — **R3 PR2 (PR #43) 에서 해결됨.**
  improvement_goals[0] V1 키 → V2 DB 키 (kpi·goal_description·roadmap_as_is·roadmap_to_be) 우선으로 정정. **R4 범위에서 제외.**

## 브랜치/PR
- 브랜치: fix/roadmap-pbl-bugs
- PR 제목: fix: 인터뷰 차수추가·비고작성·탭전환·HWPX 플레이스홀더 결함
- PR 본문에 닫는 검수 항목 번호 명시

## 절차
- 각 항목 superpowers:systematic-debugging 으로 RCA. 임시 우회 금지, 근본 원인 해결.
- HWPX 검증은 npm run dev:hwpx + npm run dev:with-hwpx 브리지 서버로 로컬 확인
- PBL 동일 영향 grep 확인 → 함께 처리
- 실패 재현 테스트(레드) → 수정(그린) → 모든 check pass → findings.md 업데이트 → 머지 대기
