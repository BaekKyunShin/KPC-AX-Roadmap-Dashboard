/**
 * @deprecated 2026-04-22 (ISSUE-02·03 Step E):
 *   원본은 `src/lib/fixtures/pbl-interview-sample.ts` 로 이동했다.
 *   `src/` 코드가 `e2e/` 를 import 하지 않도록 정식 위치는 `src/lib/fixtures/`.
 *   본 파일은 e2e 테스트 호환을 위한 re-export 만 유지한다.
 */
export {
  PBL_INTERVIEW_SAMPLE,
  type PBLInterviewSample,
} from '@/lib/fixtures/pbl-interview-sample';
