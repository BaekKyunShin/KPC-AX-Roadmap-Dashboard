# Batch 2 — UI/UX (16건)

> ⚠️ **Batch 1 PR 머지 완료 확인 후** 새 Claude Code 세션에서 시작한다.

새 세션을 열고 아래 코드블록 전체를 첫 메시지로 붙여넣는다.

**처리 대상**: ISSUE-01·02·03·05·08~18·**20** (16건)
**브랜치**: `fix/batch-2-ui-ux`
**공통 테스트 계정**:
- 운영관리자: `son@test.com` / `aaaa00000`
- 컨설턴트: `kpc@test.com` / `aaaa0000`

---

```
## 작업 맥락 (중요 - 먼저 읽기)

이 작업은 2026-04-21 QA 에서 발견된 20건 이슈 중 Batch 2 (UI/UX 16건) 를 처리하는 세션이다.
전체 20건은 수정 규모가 커서 2개 배치로 분할되어 있다:

- Batch 1 (이미 별도 세션·PR 로 완료, 4건): ISSUE-04·06·07·19 (LLM/스키마 Critical — fix/batch-1-llm-and-schema 브랜치에서 작업 후 main 에 머지 완료되어 있어야 함)
- Batch 2 (이 세션, 16건): ISSUE-01·02·03·05·08~18·20 (UI/UX)
  · ISSUE-20 은 Batch 1 Preview 검증 중 Playwright MCP 로 발견된 console error 건 (하이드레이션 #418 + 401/400 fetch)

브랜치 전략: Batch 2 → fix/batch-2-ui-ux → PR → main 머지

### 🚨 세션 시작 시 필수 확인 (중단 조건)

1. git checkout main && git pull origin main 실행
2. git log --oneline -20 에 다음 3개 머지 커밋이 모두 존재해야 Batch 2 진행 가능:
   - `134fb25` fix: Batch 1 — LLM/스키마 Critical 복구 (#18)
   - `069df4f` test: 커버리지 branches 마진 확보 (#19)
   - E2E 회귀 수정 커밋 (#20 이후, fix/ci-e2e-regressions)
3. 위 커밋이 확인되지 않으면 즉시 사용자에게 보고하고 작업 중단 (Batch 2 는 Batch 1 스키마 변경에 의존하므로 먼저 진행 불가)
4. Batch 1 에서 interview-roadmap.ts 에 competencyModelSchema 가 추가되어 있는지 grep 으로 재확인

### ⚠️ CI 커버리지 threshold 경고 (절대 완화 금지)

- `vitest.config.ts` thresholds: **lines 89 / branches 83 / functions 86 / statements 89**
- 현재 마진: branches 83.74%, functions 86.76% — **0.7% 정도의 좁은 마진**
- Batch 2 작업에서 **테스트를 삭제·skip 하면 CI 가 실패할 가능성** (특히 branches)
- 새 UI 컴포넌트를 추가할 때는 **반드시 동일 범위의 테스트 추가**. 테스트 없이 컴포넌트만 추가 금지.
- 만약 임시로 커버리지가 떨어지면 PR 생성 전에 테스트 보강해서 83%+ 복구해야 함.
- **threshold 를 내리는 옵션은 금지** — 근본적으로 테스트를 추가해 해결 (root-cause preference)

---

docs/reports/2026-04-21-post-merge-qa-issues.md 를 정독해. 아래 16건만 처리:
- ISSUE-01 (공지 첨부 파일명 깨짐)
- ISSUE-02 (/test-pbl 안내 문구)
- ISSUE-03 (/test-pbl·roadmap 샘플 데이터 버튼)
- ISSUE-05 (프로젝트 상세 트랙 배지)
- ISSUE-08 (활동 일지 아이콘)
- ISSUE-09 (인터뷰 textarea 높이)
- ISSUE-10 (수행시간 시작~종료 범위)
- ISSUE-11 (AI 필요도 라벨 + Export 동일 적용)
- ISSUE-12 (작성 가이드 머릿기호)
- ISSUE-13 (작성 가이드 인덴테이션)
- ISSUE-14 (첨부 URL → 파일 + 파싱 + LLM 통합)
- ISSUE-15 (다음 클릭 스크롤)
- ISSUE-16 (STT 업로드 복원)
- ISSUE-17 (인터뷰 기록 탭 전체 노출)
- ISSUE-18 (로드맵/PBL 초기 생성 버튼)
- ISSUE-20 (console error — 하이드레이션 #418 + 401/400 fetch)
  · 우선 DevTools Network 탭에서 실패 요청의 url/payload 확인으로 시작
  · 하이드레이션 불일치는 sidebar/헤더/알림 뱃지 등 사용자 컨텍스트 SSR/CSR 경계 재검토
  · 기능 영향 Low — 다른 UI 항목 작업 완료 후 마지막 카테고리로 처리 권장

스크린샷: docs/screenshots/2026-04-21-qa/
산인공 공식 양식: docs/references/1.*.hwp + 1.*.pdf (로드맵), 2.*.hwp + 2.*.pdf (PBL)
배포·완료 기준 계획서: docs/plans/archive/2026-04-20-ofa-post-merge-deployment.md §7 "2차 완료 기준"

## 필수 사용 스킬 (생략 금지)
- superpowers:writing-plans → Plan Mode 에서 카테고리별 Step 계획 수립
- superpowers:test-driven-development → 로직 변경은 반드시 TDD (단순 CSS 수정 제외)
- superpowers:executing-plans → 계획 실행
- superpowers:verification-before-completion → 완료 선언 전 필수
- frontend-guide → UI 컴포넌트 작성/수정 시
- web-design-guidelines → UI 검수 (접근성·UX 감사)
- react-best-practices → React/Next.js 성능 최적화
- composition-patterns → 컴포넌트 구조 리팩터링 시
- check-server-action → Server Action 수정 시 (ISSUE-01·10·14·16)
- supabase-dev → 마이그 필요 시 (ISSUE-10·14)
- refactoring → GREEN 단계 직후

## 필수 사용 MCP
- mcp__supabase__apply_migration → 마이그 SQL 작성 시 직접 DB 적용 (수동 실행 금지)
- mcp__supabase__list_migrations → 적용 검증
- mcp__plugin_playwright_playwright__* → 매 Step 탐색적 회귀 + 신규 기능 즉흥 확인
  • 운영관리자: son@test.com / aaaa00000
  • 컨설턴트: kpc@test.com / aaaa0000
- mcp__plugin_serena_serena__* → 기존 코드 심볼 탐색/편집
- mcp__plugin_context7_context7__query-docs → 새 라이브러리(pdf-parse·mammoth 등) 사용 시 최신 문서
- mcp__shadcn__* → shadcn/ui 컴포넌트 추가 시

## 서브에이전트 (필요 시)
- performance-engineer → ISSUE-14 파일 파싱 파이프라인 성능
- test-automator → Playwright E2E 시나리오 작성 + UI 변경에 따른 기존 spec selector 업데이트
- code-reviewer → 각 카테고리 완료 후 리뷰

## 진행 순서
1. Plan Mode 진입 → 카테고리별 Step 으로 계획 수립 (제안):
   - Step A: 스키마/DB (ISSUE-10 수행시간 범위, ISSUE-14 첨부 파일 필드)
   - Step B: 파일 파싱 파이프라인 신규 (ISSUE-14, HRD4U 첨부 PDF 파싱 공유)
   - Step C: 인터뷰 UX (ISSUE-09·10·11·12·13·14·15·16)
   - Step D: 프로젝트 상세/기록 (ISSUE-05·17·18)
   - Step E: 테스트 페이지 (ISSUE-02·03)
   - Step F: 활동일지·공지 (ISSUE-01·08)
   - Step G: console error 정리 (ISSUE-20 — 하이드레이션 #418 + 401/400 fetch)
     · DevTools Network 탭에서 401/400 요청의 url/payload 확인
     · `/` redirect 로직 점검 (middleware.ts 또는 src/app/page.tsx)
     · 하이드레이션 불일치: `useEffect` 로 클라이언트 값만 렌더하는 부분에 `suppressHydrationWarning` 또는 `'use client'` 경계 재검토
     · 최종 검증: Playwright MCP 로 컨설턴트·운영관리자 플로우 console error 0건

   ⚠️ 계획서에 각 Step 별로 아래 4가지를 반드시 명시할 것:
   1. 사용할 스킬·MCP 도구·서브에이전트
      (예: "Step B: frontend-guide 스킬 + mcp__plugin_context7_context7__query-docs + performance-engineer 서브에이전트")
   2. 회귀 테스트 범위 — 해당 Step 이 건드리는 파일/기능 + 영향 받을 수 있는 연관 기능 목록
      (예: "Step C 회귀 범위: 인터뷰 전체 폼 동작·자동저장·로드맵 생성 버튼, 텍스트 입력 폼 높이 변경이 모바일 뷰에 미치는 영향")
   3. 회귀 실행 방법 — Step 커밋 전 반드시 수행:
      - 코드 회귀: npm run test -- <관련 파일> + npm run typecheck + npm run lint
      - E2E 회귀 (npm run test:e2e — 자동화된 회귀 자산):
        · 매 Step 커밋 전: 해당 Step 관련 spec 만 실행
        · 매 Step 마다 Batch 1 Critical 관련 spec (매칭·사전분석·로드맵 생성) 반드시 재실행 — UI 수정이 LLM 기능을 다시 깨뜨리지 않았는지 확인
        · PR 생성 전 최종: npm run test:e2e 전체 실행
      - UI 회귀 (Playwright MCP): E2E spec 으로 아직 커버 안 된 신규 기능을 Claude 가 실시간 조작해 확인, 콘솔 에러 0건
      - 모바일 뷰포트(375×667) 영향 범위 검증 (ISSUE-09 등) — Playwright MCP 로 수동 또는 e2e/mobile.*.spec.ts 추가
   4. E2E spec 동기화 범위 — 기능·UI·스키마 수정 Step 에서 관련 spec 도 같은 커밋에 포함 수정:
      (a) Selector 업데이트: 텍스트·DOM 만 바뀐 경우
          (예: ISSUE-11 "1 불필요" → "1 매우 낮음", ISSUE-12 머릿기호 숫자 → 불릿)
      (b) 시나리오 확장: 신규 기능 추가
          (예: ISSUE-16 STT 업로드 복원 시나리오, ISSUE-14 파일 업로드·파싱 검증, ISSUE-03 샘플 데이터 채우기 버튼)
      (c) 시나리오 재작성: 기능 철학 변경
          (예: ISSUE-14 URL 첨부 → 실제 파일 업로드 → LLM 반영 검증, ISSUE-10 단일 시간 → 시작·종료 범위)
      - PBL/운영관리자 동일 기능 변경 시 해당 spec 도 업데이트 (OPS/PBL 측 spec 포함)
      - test-automator 서브에이전트 활용
      ⚠️ 금지: selector 만 바꿔서 pass 만 맞추는 눈속임. spec 이 여전히 "실제 사용자 플로우" 를 검증하는지 검토
      ⚠️ 필수: 계획서의 "E2E spec 수정 범위" 항목에 (a)/(b)/(c) 분류해 사전 명시
   ⚠️ 회귀 실패 시 해당 Step 커밋 절대 금지, 원인 해결 후 재수행. 임시 skip·주석처리 금지.

2. 계획 내가 승인하면:
   - 브랜치 생성: fix/batch-2-ui-ux
   - Step 별 커밋 (여러 이슈 묶지 말 것)
   - 마이그 필요 시 mcp__supabase__apply_migration 으로 직접 DB 적용
   - 매 Step 커밋 전 위 회귀 3종(코드·E2E·Playwright MCP) 통과 + E2E spec 동기화 통과 확인

3. 완료 후 검수 (셋 다 필수):
   a. 코드 검수:
      - npm run validate (typecheck + lint + test) 통과
      - npm run build 통과
      - web-design-guidelines 스킬로 UI 접근성·UX 감사
      - code-reviewer 서브에이전트로 PR 수준 리뷰
   b. E2E 전체 회귀:
      - npm run test:e2e 전체 실행 → 통과
      - 신규/변경 UI 에 맞춰 기존 spec selector 업데이트 완료
      - Batch 1 Critical 관련 spec (매칭·사전분석·로드맵 생성) 통과
      - 신규 기능(파일 파싱·STT 등) spec 추가되어 있을 것
   c. Playwright MCP 탐색적 회귀 (15건 재현 경로 + 15건 외 영향 범위):
      - 컨설턴트(kpc@test.com/aaaa0000) 시나리오: 인터뷰 전체 플로우 · 갤러리 · 공지 · /test-pbl · /test-roadmap
      - 운영관리자(son@test.com/aaaa00000) 시나리오: 프로젝트 상세 배지 · 활동일지
      - 모바일 뷰포트(375×667)도 주요 화면 검증 (ISSUE-09 등)
      - Batch 1 Critical 기능 3종(매칭·사전분석·로드맵 생성) 여전히 정상 동작 확인
      - 콘솔 에러 0건 확인
4. 셋 다 통과하면 PR 생성 제안 (제목·본문·테스트 계획 초안 포함)
   - 계획서 §7 "2차 완료 기준" 체크리스트 참조해 PR 본문에 체크박스 포함

## 규칙
- 🚨 최우선: 모든 Step 마다 회귀 테스트 필수 — 코드 회귀(test + typecheck + lint) + E2E 회귀(npm run test:e2e, 관련 spec 만) + Playwright MCP 탐색적 회귀 + E2E spec 동기화 수정 전부 통과해야 커밋. 실패 시 커밋 금지. Batch 1 에서 복구한 Critical 기능(매칭·사전분석·로드맵 생성)이 다시 깨지지 않았는지 매 Step 확인. 회귀 범위·실행 명령·spec 수정 분류(a/b/c)는 계획서에 사전 명시
- verification-before-completion 스킬 호출 전에는 "완료" 선언 금지
- PBL 트랙·운영관리자 계정에 동일 UI/메뉴 있으면 함께 일관 수정 (해당 spec 파일도 동기화)
- HWPX/PDF/XLSX export 에도 라벨 변경 반영 (ISSUE-11)
- ISSUE-16 STT 복원은 git log 로 과거 구현 참고 (history 스킬 탐색)
- 검증 실패 시 즉시 보고 (임시 우회·skip 금지)
- 한국어로 답변·커밋 메시지 작성
```
