# 2026-04-19 OFA Step 12 Task 8 — UI/UX 최종 감사 리포트

## 감사 기준

- 자체 `frontend-guide` + 공용 `web-design-guidelines`
- Session 10 (Step 11) 이 도입한 컨설턴트 네비 드롭다운 UX 체크포인트

## 감사 결과 요약

| 영역 | 결과 |
|------|------|
| 드롭다운 ARIA 속성 | ✅ 수정 완료 (`aria-haspopup="menu"` 추가) |
| Escape 키 닫기 | ✅ `Navigation.tsx:221` |
| 바깥 클릭 닫기 | ✅ `Navigation.tsx:209`  |
| `aria-expanded` | ✅ 3곳 모두 적용 |
| `TrackBadge` size prop 일관성 | ✅ (갤러리 카드·프로젝트 테이블·상세 모두 `md` 기본) |
| 공용 디자인 키트 재사용 | ✅ `src/components/roadmap/shared/` 양쪽 트랙에서 사용 |
| 모바일 아코디언 네비 | ✅ `MobileNavGroup` 존재 |
| HWPX 파일명 중앙화 | ✅ `hwpx-filename.ts` + 통일 규칙 |

## 수정 내용 (본 Step 12)

1. `src/components/Navigation.tsx` — 드롭다운 토글 버튼 3개 (데스크톱 그룹, 모바일 아코디언 그룹, 사용자 메뉴) 에 `aria-haspopup="menu"` 추가. 기존의 `aria-expanded` 와 병행 선언되어 스크린리더가 드롭다운임을 명확히 인식한다.

## 남은 Medium/Low (Session 1~10 감사에서 발견, 본 PR 범위 밖)

다음은 Task 3.7 코드 감사 결과로 기록된 항목 중 UI 성격:

- `AttachmentList` 가 네이티브 `window.confirm()` 사용 — shadcn `AlertDialog` 로 마이그 권장 (Low)
- `fetchGalleryPBLReports` / `fetchPBLReportDetail` 의 `PBLContent` 탐색 로직 중복 — helper 추출 리팩토링 (Low)

위 항목은 본 OFA 프로젝트의 양식 정렬 목표와 직접 관련 없으므로 별도 hotfix PR 로 처리.

## 결론

**본 PR의 UI 품질은 프로덕션 반영 가능한 수준**. 드롭다운 ARIA 속성 보강으로 접근성 준수 향상됨. 사용자 시각 QA 에서 추가 수정이 필요하면 hotfix PR 로 처리.
